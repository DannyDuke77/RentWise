# Core Django & runtime
Django>=4.2,<6.0             # Main web framework (restricted for dj-rest-auth compatibility)
asgiref==3.11.0              # ASGI reference implementation (async support)
sqlparse==0.5.5              # SQL parsing (used by Django)
tzdata==2025.3               # Timezone database (required on some systems)
django-extensions==4.1

# Background tasks
celery
redis

# Security
cryptography==50.0.1         # Cryptographic library

# Environment & utilities
python-dotenv==1.2.1         # Load environment variables from .env files
requests>=2.31               # HTTP client for external API calls

# Database
psycopg2-binary==2.9.11      # PostgreSQL database adapter

# Django REST & Authentication
djangorestframework==3.16.1  # Django REST API framework
dj-rest-auth>=7.1.0,<8.0.0   # Authentication endpoints (login, logout, password reset)
django-allauth==65.13.1      # User registration, social auth, email verification
djangorestframework-simplejwt==5.5.1  # JWT authentication support

# CORS & frontend integration
django-cors-headers==4.9.0   # Cross-Origin Resource Sharing (CORS) handling

# Media & files
Pillow==12.0.0 

ReportLab==4.4.9         # PDF generation library

# Resend
resend==2.42.0